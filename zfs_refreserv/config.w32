// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("zfs_refreserv", "for zfs_refreserv support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("zfs_refreserv", "enable zfs_refreserv support", "no");

if (PHP_ZFS_REFRESERV != "no") {
	EXTENSION("zfs_refreserv", "zfs_refreserv.c");
}

