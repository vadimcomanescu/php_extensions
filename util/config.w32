// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("util", "for util support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("util", "enable util support", "no");

if (PHP_UTIL != "no") {
	EXTENSION("util", "util.c");
}

